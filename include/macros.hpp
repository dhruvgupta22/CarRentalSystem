# pragma once

/* This file contains all the Hyper Parameters used */

#define FINE_PER_DAY 100 // Fine (in $) per day for late returning 
#define MAX_RENT_LIMIT 7 // Max number of days for which car can be rented
#define EMPLOYEE_DISCOUNT 15 // Employee discount(%) on car rent
#define DEFAULT_RECORD 5 // Default record for a new user